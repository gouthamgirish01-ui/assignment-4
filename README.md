# assignment-4
in the second assignment :
  i created the file first in w mode the write data into it and also add \n at the end for newline 
  then opened in a mode for appending the added the data from used after including \n at back 
  then take data from file using read after opening it in r mode 
  the print it 

  
  
